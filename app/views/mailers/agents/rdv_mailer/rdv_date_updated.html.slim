div
  p Bonjour,
  p
    | Un RDV qui devait avoir lieu #{relative_date @old_starts_at} à #{l(@old_starts_at, format: "%H:%M")} vient d'être modifié par #{@author.full_name}. La date de ce RDV a changé, voir ci-dessous:
    = render "mailers/common/rdv_overview", rdv: @rdv, for_role: :agent

  p.aligncenter
    = link_to "Voir sur RDV-Solidarités", admin_organisation_agent_url(@rdv.organisation_id , @agent),  class: 'btn btn-primary'
