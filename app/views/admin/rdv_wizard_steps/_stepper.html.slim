ul.list-group.list-group-flush
  - steps.each do |step|
    - if step[:value].present?
      li.list-group-item.hover-bg-light
        = link_to rdv_wizard_form.path_for_step(step[:step]), class: "text-dark" do
          .d-flex.align-items-center
            i.far.mr-1.fa-check-circle
            div.flex-shrink-0.mr-1= t("admin.rdv_wizard_steps.step#{step[:step]}.card_title")
            div.text-muted.text-truncate= step[:value]
    - else
      li.list-group-item
        .d-flex.align-items-center
          i.far.mr-1.fa-circle
          div.flex-shrink-0.mr-1= t("admin.rdv_wizard_steps.step#{step[:step]}.card_title")
          div.text-muted.text-truncate= step[:value]
