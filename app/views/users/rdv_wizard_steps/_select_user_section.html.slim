- relatives = @rdv_booking_form.selectable_existing_relatives

fieldset.fr-fieldset.rdv-proche-selection.fr-mt-3w data-controller="proche-selection" data-action="change->proche-selection#update"
  legend.fr-fieldset__legend--regular.fr-fieldset__legend Pour qui prenez-vous rendez-vous ?

  .fr-fieldset__element.rdv-nested-fields-wrapper data-proche-selection-target="option"
    .fr-radio-group
      = radio_button_tag "selected_users[]", "current_user", @rdv_booking_form.selected_users.include?("current_user"), id: "selected_user_current_user"
      = label_tag "selected_user_current_user", "Moi-même"
    - if @rdv_booking_form.requires_ants_predemande_number?
      .rdv-nested-fields
        = f.dsfr_text_field :ants_pre_demande_number

  - relatives.each do |relative|
    .fr-fieldset__element.rdv-nested-fields-wrapper data-proche-selection-target="option"
      .fr-radio-group
        = radio_button_tag "selected_users[]", "existing_relative_#{relative.id}", @rdv_booking_form.selected_users.include?("existing_relative_#{relative.id}"), id: "selected_user_existing_relative_#{relative.id}"
        = label_tag "selected_user_existing_relative_#{relative.id}", relative.full_name
      - if @rdv_booking_form.requires_ants_predemande_number?
        .rdv-nested-fields
          = f.fields_for :relatives, [relative] do |rf|
            = rf.hidden_field :id
            = rf.dsfr_text_field :ants_pre_demande_number

  .fr-fieldset__element.rdv-nested-fields-wrapper data-proche-selection-target="option"
    .fr-radio-group
      = radio_button_tag "selected_users[]", "new_relative_0", @rdv_booking_form.selected_users.include?("new_relative_0"), id: "selected_user_new_relative_0"
      = label_tag "selected_user_new_relative_0", "Une autre personne"

    .rdv-nested-fields
      = f.fields_for :relatives, [@rdv_booking_form.new_proche] do |rf|
        .fr-grid-row.fr-grid-row--gutters
          .fr-col-12.fr-col-md-6= rf.dsfr_text_field :first_name, label: "Prénom"
          .fr-col-12.fr-col-md-6= rf.dsfr_text_field :last_name, label: "Nom"
          - if @rdv_booking_form.show_birth_date_field?
            .fr-col-12.fr-col-md-6.fr-mt-4w= rf.dsfr_text_field :birth_date, type: "date", label: "Date de naissance"
          - if @rdv_booking_form.requires_ants_predemande_number?
            .fr-col-12= rf.dsfr_text_field :ants_pre_demande_number
