- if rdv.cancelled?
  h1 ANNULÉ : RDV du #{l(rdv.starts_at, format: :human)}

- elsif previous_starts_at.present?
  h1 Modification de votre RDV
  p
    | Votre rendez-vous initialement prévu le #{l(previous_starts_at, format: :human)} a été déplacé au&nbsp;
    strong #{l(rdv.starts_at, format: :human)}
    |.
- else
  h1 RDV confirmé le #{l(rdv.starts_at, format: :human)}
